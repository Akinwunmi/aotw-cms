export enum RouteAdmin {
  AdminIdentifier = 0,
  EntityIdentifier = 1,
  Mode = 2,
  Entity = 3,
}

export enum RouteDiscover {
  DiscoverIdentifier = 0,
  EntityIdentifier = 1,
  Entity = 2
}

export enum RouteSearch {
  Search = 0
}
